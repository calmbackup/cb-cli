use ratatui::Frame;
use ratatui::layout::Rect;
use crate::tui::app::App;

/// Render a yes/no confirmation dialog overlay.
pub fn draw(app: &App, frame: &mut Frame, area: Rect) {
    todo!("Message + [No] [Yes] buttons with highlight")
}
